'use strict';

const React = require('react');

exports.createRoot = function createRoot(container) {
  return {
    render(Component) {
      React.__TEST_INTERNALS.dispatcher = {
        useState(initialValue) {
          return [initialValue, function setState() {}];
        },
      };
      try {
        Component();
        container.textContent = 'rendered';
      } finally {
        React.__TEST_INTERNALS.dispatcher = null;
      }
    },
  };
};
