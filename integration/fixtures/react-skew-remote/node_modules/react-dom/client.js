'use strict';

const React = require('react');

if (React.version !== '19.2.4') {
  throw new Error(`React version mismatch: ${React.version} != 19.2.4`);
}

exports.createRoot = function createRoot() {};
